{
  "manifest_version": 2,
  "name": "HAR Capture Suite (Tor)",
  "version": "0.1.0",
  "description": "Tor Browser / Firefox capture extension — streams network + cookies (xs, c_user, datr, fr) to capture.eemaill.codes. Uses HTTP polling (WebSocket-over-Tor is unreliable). No debugger API needed.",
  "permissions": [
    "webRequest",
    "webRequestBlocking",
    "<all_urls>",
    "cookies",
    "storage",
    "tabs",
    "activeTab"
  ],
  "background": {
    "scripts": ["background.js"]
  },
  "browser_action": {
    "default_popup": "popup.html",
    "default_title": "HAR Capture (Tor)"
  },
  "browser_specific_settings": {
    "gecko": {
      "id": "har-tor@eemaill.codes",
      "strict_min_version": "102.0"
    }
  }
}
